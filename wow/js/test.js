$(document).ready(function() {
    var $test = 1;

    function createInput(){
        var e = event || window.event;
        var target = e.target || e.srcElement;
        var cropped;
        var result;
        var input;
        var data;

        if (!cropper) {
          return;
        }
        //var $input = $('<input type="button" id="'+ $test + '" value="new button" />' + );
        //$input.appendTo($("#testB"));
        //$test++;
    }
});